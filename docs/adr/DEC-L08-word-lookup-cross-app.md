---
status: accepted
---

# Word lookup is a separate cross-app feature

Full word lookup (tap token → definition panel, hover tooltip on desktop, sticky panel on mobile) is designed as a standalone `WordLookup` component usable across Learn, Capture, and Dictionary. Built after Learn v1 ships. Learn v1 uses a simplified placeholder (tap → opens Dictionary tab).

**Date:** 2026-05-26
