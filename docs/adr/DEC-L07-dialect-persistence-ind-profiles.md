---
status: accepted
---

# Dialect persistence: `ind_profiles.default_dialect`

The dialect selected in Learn is persisted in `ind_profiles.default_dialect`. The stored value is the Chinese dialect name matching `occurrences.dialect_name` directly (e.g. `"南勢阿美語"`).

**Date:** 2026-05-26
