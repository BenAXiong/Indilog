# Design questions

Open questions that need real-world data or cross-cutting discussion before a decision can be made. Each links to the ADR where the full context lives.

> Note: questions scoped to a single algorithm or subsystem live *inside* the relevant ADR (e.g. the Again/`repetitions` question is in `docs/adr/DEC-SRS03-formosrs1-algorithm.md`). This file is for cross-cutting questions that don't belong to one ADR.

| Question | Context ADR | Status |
|---|---|---|
| Remove Easy from test pass? (exposure-Easy already covers "instant recognise"; test-pass Easy may be overconfident — or it could be useful for cards where the user immediately recognises without needing the flip) | [DEC-M5-01](docs/adr/DEC-M5-01-learn-review-separation.md) | needs session data |
